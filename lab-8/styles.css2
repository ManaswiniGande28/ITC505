/* Reset default browser styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body styling */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #e8f5e9; /* Light green background */
    color: #333;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

/* Title */
h2 {
    font-size: 28px;
    color: #4CAF50; /* Green color */
    text-align: center;
    margin-bottom: 20px;
    font-weight: 600;
}

/* Form container */
#registration-form {
    background-color: #fff;
    padding: 40px;
    width: 100%;
    max-width: 480px;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

/* Styling the labels */
label {
    font-size: 16px;
    color: #333;  /* Dark color for labels */
    margin-bottom: 8px;
    display: block;
    font-weight: 500;
}

/* Input field styling */
input[type="text"],
input[type="email"],
input[type="password"] {
    width: 100%;
    padding: 12px;
    margin: 10px 0 20px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 16px;
    transition: all 0.3s ease;
}

/* Apply colors to different fields */
input[type="text"]:focus, /* First and Last Name */
input[type="email"]:focus, /* Email */
input[type="password"]:focus {  /* Password */
    border-color: #4CAF50;  /* Green focus for inputs */
    outline: none;
    box-shadow: 0 0 5px rgba(76, 175, 80, 0.3);
}

/* Specific input field border color */
input[type="text"] {
    border-color: #4CAF50;  /* Green border for First and Last Name */
}

input[type="email"] {
    border-color: #2196F3; /* Blue border for email */
}

input[type="password"] {
    border-color: #FF9800; /* Orange border for password */
}

/* Submit button */
button {
    width: 100%;
    padding: 12px;
    background-color: #4CAF50;
    color: #fff;
    border: none;
    border-radius: 8px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #45a049;
}

/* Error messages */
.error-message {
    color: #d8000c;
    font-size: 14px;
    margin-top: -10px;
    margin-bottom: 15px;
}

/* Alert box for validation errors */
.alert {
    background-color: #f8d7da;
    color: #721c24;
    padding: 10px;
    border-radius: 8px;
    margin-bottom: 20px;
}

/* Responsive design for mobile devices */
@media (max-width: 768px) {
    #registration-form {
        padding: 20px;
    }

    h2 {
        font-size: 24px;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"],
    button {
        padding: 10px;
        font-size: 14px;
    }
}

/* Smooth transition for form interactions */
input[type="text"],
input[type="email"],
input[type="password"],
button {
    transition: all 0.3s ease;
}
